spring:
  application:
    name: af-novadesk-api

  # ── Exclude database auto-configuration ────────────────────────────────────
  # af-novadesk-api has no database. The parent POM (af-spring-parent) pulls in
  # JPA, Flyway and PostgreSQL transitively — exclude them to prevent startup
  # failure when no datasource URL is configured.
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
      - org.springframework.boot.autoconfigure.jdbc.DataSourceTransactionManagerAutoConfiguration
      - org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration
      - org.springframework.boot.autoconfigure.data.jpa.JpaRepositoriesAutoConfiguration
      - org.springframework.boot.autoconfigure.flyway.FlywayAutoConfiguration
  jackson:
    serialization:
      write-dates-as-timestamps: false
      indent-output: true
    property-naming-strategy: SNAKE_CASE
  boot:
    admin:
      client:
        url: ${SPRING_BOOT_ADMIN_URL}
        username: ${SPRING_BOOT_ADMIN_CLIENT_USERNAME}
        password: ${SPRING_BOOT_ADMIN_CLIENT_PASSWORD}
        instance:
          prefer-ip: true
management:
  server:
    port: ${SERVER_PORT}
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
      base-path: /actuator
  endpoint:
    health:
      show-details: always
      show-components: always
      probes:
        enabled: true
      group:
        liveness:
          include: livenessState,ping,diskSpace
        readiness:
          include: readinessState
    info:
      enabled: true
  info:
    build:
      enabled: true
    git:
      enabled: true
      mode: full
    java:
      enabled: true
    os:
      enabled: true
    env:
      enabled: true
  metrics:
    export:
      prometheus:
        enabled: true
  health:
    circuitbreakers:
      enabled: false
info:
  application:
    name: ${spring.application.name}
    version: "@project.version@"
    description: "AF NovaDesk API"
  build:
    timestamp: "@maven.build.timestamp@"
    java-version: "@java.version@"
server:
  port: ${SERVER_PORT}
  servlet:
    context-path: /novadesk
logging:
  level:
    root: INFO
